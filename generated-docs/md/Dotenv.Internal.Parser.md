## Module Dotenv.Internal.Parser

This module encapsulates the parsing logic for a `.env` file.

#### `parser`

``` purescript
parser :: Parser String (Array (Setting UnresolvedValue))
```

Parses `.env` settings.


